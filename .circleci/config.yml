version: 2.1

jobs:
  build:
    docker:
      - image: openjdk:18.0.2

    steps:
      - checkout
      - run:
          command: chmod +x mvnw
      - run:
          name: Build
          command: ./mvnw clean package